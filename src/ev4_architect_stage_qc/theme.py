from tkinter import ttk
def apply(root):
 style=ttk.Style(root); style.configure('Title.TLabel',font=('Segoe UI',15,'bold')); style.configure('Status.TLabel',padding=(4,6)); style.configure('TButton',padding=(8,5))
